import React from 'react';
import { SelfApp } from '@selfxyz/common/utils/appType';
export { SelfApp, SelfAppBuilder } from '@selfxyz/common/utils/appType';

interface SelfQRcodeProps {
    selfApp: SelfApp;
    onSuccess: () => void;
    onError: (data: {
        error_code?: string;
        reason?: string;
    }) => void;
    type?: 'websocket' | 'deeplink';
    websocketUrl?: string;
    size?: number;
    darkMode?: boolean;
    children?: React.ReactNode;
}
declare const SelfQRcodeWrapper: (props: SelfQRcodeProps) => React.JSX.Element;
declare const SelfQRcode: ({ selfApp, onSuccess, onError, type, websocketUrl, size, darkMode, }: SelfQRcodeProps) => React.JSX.Element;

export { SelfQRcode, SelfQRcodeWrapper };
