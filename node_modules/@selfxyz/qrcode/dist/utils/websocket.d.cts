import { SelfApp } from '@selfxyz/common/utils/appType';

interface WebAppInfo {
    appName: string;
    userId: string;
    logoBase64: string;
}
declare function initWebSocket(websocketUrl: string, selfApp: SelfApp, type: 'websocket' | 'deeplink', setProofStep: (step: number) => void, onSuccess: () => void, onError: (data: {
    error_code?: string;
    reason?: string;
}) => void): () => void;

export { type WebAppInfo, initWebSocket };
